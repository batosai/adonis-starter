@set('defaultClasses', 'block px-4 py-2 mt-4 w-full text-sm font-medium leading-5 text-center text-white transition-colors duration-150 bg-purple-600 border border-transparent rounded-lg active:bg-purple-600 hover:bg-purple-700 focus:outline-none focus:shadow-outline-purple')

<button
  type="submit"
  {{ $props.serialize({ class: [classes, defaultClasses] }, false) }}
>
  @if(text)
    {{ text }}
  @else
    {{{ await $slots.main() }}}
  @end
</button>
